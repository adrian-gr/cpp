# Design Patterns

Design patterns are grouped into the three classic categories:

- `creational/` - object creation and construction techniques
- `structural/` - composition of objects and types
- `behavioral/` - communication and responsibility between objects

Each category will contain focused C++ examples with guidance on when to use a pattern and what tradeoffs it introduces.
